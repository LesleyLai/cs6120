# CMD: bril2json < {filename} | cargo run -p bril-lvn -- {args} | cargo run -p bril-tdce -- {args} | bril2txt

@main {
  a: int = const 4;
  b: int = const 2;
  sum1: int = add a b;
  sum2: int = add a b;
  prod: int = mul sum1 sum2;
  print prod;
}
